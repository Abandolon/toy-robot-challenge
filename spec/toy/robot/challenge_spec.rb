# frozen_string_literal: true

RSpec.describe Toy::Robot::Challenge do
  it "has a version number" do
    expect(Toy::Robot::Challenge::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
